apiVersion: v1
kind: Service
metadata:
  labels:
    app: nextcloud-database
  name: nextcloud-database
spec:
  selector:
    app: nextcloud-database
  type: ClusterIP
  ports:
    - name: database
      port: 3306
      targetPort: mysql
      protocol: TCP
