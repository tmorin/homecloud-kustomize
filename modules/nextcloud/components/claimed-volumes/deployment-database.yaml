apiVersion: apps/v1
kind: Deployment
metadata:
  name: nextcloud-database
spec:
  template:
    spec:
      volumes:
        - name: data
          emptyDir:
          persistentVolumeClaim:
            claimName: nextcloud-database
